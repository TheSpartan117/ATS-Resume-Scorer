# ATS Resume Scorer - Environment Variables Example
# Copy this file to .env and update with your values

# Environment
ENVIRONMENT=development

# Backend Configuration
BACKEND_URL=http://localhost:8000

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:8080

# OnlyOffice Document Server Configuration
ONLYOFFICE_SERVER_URL=http://localhost:8080
ONLYOFFICE_JWT_SECRET=your-secret-key-change-in-production

# Database Configuration (if using)
# DATABASE_URL=postgresql://user:password@localhost/ats_resume_scorer

# Authentication (if using)
# JWT_SECRET=your-auth-jwt-secret
# JWT_ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30
